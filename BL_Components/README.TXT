IPFS Storage
- To run: npm install -> npm start

Smart Contract are responsible for:
- Stores hash for the IPFS url of the text document
- Stores a list of user address with access to the documents
- Calls ML function to determine if plagarism is detected
- Returns to user the hash of their text document if storing on the IPFS was successful, else returns plagarism detected